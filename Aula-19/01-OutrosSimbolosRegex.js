// ^<   ^ -> so casa com o incio da string

// $ -> so casa com o fim da string, vc usa ele de forma invertida. >$. Ele sempre vem no final da call

//inicio e o fim /(^<).+(>$)/ //verificando o inicio e fechamendo da string, nao tem espaco nessa buceta, para de dar espaco
