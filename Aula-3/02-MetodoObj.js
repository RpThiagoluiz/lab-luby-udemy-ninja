/*
  Adc um nova propriedade dentro do object
   pelo ponto.
   pessoa = {nome:"thiago", altura:1.72}
   pessoa.cor = 'branco'
   console.log(pessoa) //tera todos os dados

   pode passa function dentro do obj
   pessoa.andar = function(){
     return 'Pessoa andando'
   }

   agora dentro de pessoa temos a function.
   pessoa.andar() //lembra de executar o metodo, porq ela é uma function.

   Queria incrementar a idade de pessoa toda vez q ela fizer aniversario
   pessoa.idade = 18

   pessoa.aniversario = function() {
     pessoa.idade ++
   }

   pessoa.aniversario()
   pessoa.idade = 19

   *1, uma function dentro de um objeto é chamada de metodo. do objeto
   *2, objeto ele tem propriedade e valor.

*/
