/*
  var a ,b = 2, c

  ta declarando varias variaveis ao mesmo tempo
  var a;
  var b = 2;
  var c;

  <-- Multiplos retornos -->

  function myFunction () {
    var x = 0
    return ( x+=1, x) //return ++x -operador de pre encremento
  }

  Na function ela vai incrementar depois retornar X igual o operador varia.
*/
