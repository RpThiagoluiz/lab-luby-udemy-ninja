(function () {
  function app() {
    //Declaracoes da variavels, vc pegando elas do html

    //function.

    return {
      //Acessar um metodo, ele retorna aquele mesmo metodo.
      //Assim ele revela os metodos . consegue acessar pelo objeto
    };
  }

  // -> igual fez com a dom para exportar ele para o escopo global window.app = app
  app();

  //app.getMessage - pelo metodo do getMessage, vc consegue acessar.
})();
